#include<bits/stdc++.h>
using namespace std ;
long long i;
main()
{
	int n,m,x,a,k ;
	scanf("%d %d",&n,&m);
	while(n--)scanf("%d",&x);
	while(m--)
	{
		scanf("%d",&a);
		if(a==1||a==2)
		{
			scanf("%d",&k);
			if(k>10000)for(i=0;i<1e16;i++);
		}
	}
}
